# Frontend (Vite) environment
# Backend API URL used by Axios client.
# Keep `/api` suffix and avoid trailing slash.
# Local:
VITE_API_BASE_URL=http://localhost:5000/api
# Production example:
# VITE_API_BASE_URL=https://api.yourdomain.com/api
